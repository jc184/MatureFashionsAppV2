@model IEnumerable<MatureFashionsAppV2.Models.DB.Shows>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ShowTime)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ShowTotalSale)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FranchiseNoNavigation)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.HomeNoNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ShowTime)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ShowTotalSale)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FranchiseNoNavigation.FranchiseNo)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.HomeNoNavigation.HomeAddress)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
            </td>
        </tr>
}
    </tbody>
</table>
