@model IEnumerable<MatureFashionsAppV2.Models.DB.Invoices>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.InvoiceDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.InvoiceDateDue)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.InvoiceNet)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FranchiseNoNavigation)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.OrderNoNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.InvoiceDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.InvoiceDateDue)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.InvoiceNet)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FranchiseNoNavigation.FranchiseNo)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.OrderNoNavigation.OrderNo)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.InvoiceNo">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.InvoiceNo">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.InvoiceNo">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
